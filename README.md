IMPORTANT NOTICE:

If you do plan on using the py version open it in visual studio and run the following commands through the terminal

pip install keyboard

pip install pyautogui

pip install time (Might not have to do this one)

Side notes:

It's not too well annotated as it is made for use rather than someone editing it other than me that is.
